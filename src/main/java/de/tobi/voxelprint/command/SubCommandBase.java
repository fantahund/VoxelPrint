package de.tobi.voxelprint.command;

import de.iani.cubesideutils.fabric.commands.SubCommand;

/**
 * The library's subcommand, named once so the import lives in one file.
 *
 * <p>Nothing is added here. It exists so that swapping the command library --
 * or dropping it -- is an edit to one class rather than to every command in the
 * mod.
 */
public abstract class SubCommandBase extends SubCommand {
}
